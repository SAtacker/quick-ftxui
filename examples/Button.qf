Vertical{
    int x = 5
    str y = "Test"
    str z = "init"
    str a
    Horizontal {
        Red Button {
            "ls -l",
            System("ls -l"),
            Animated,
            z
        }
        separator
        BlueLight Button{
            "chrome",
            System("/usr/bin/google-chrome-stable"),
            Ascii,
            z
        }
    }
    Heavy separator
    text("This button exits the TUI")
    Button {
        "Exit",
        "Exit"
    }
}

