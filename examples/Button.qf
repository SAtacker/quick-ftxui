Vertical{
    int x = 5
    str y = "Test"
    str z = "init"
    str a
    Horizontal {
        Button {
        "ls -l",
        System("ls -l"),
        Animated,
        z
        }
        Button{
            "chrome",
            System("/usr/bin/google-chrome-stable"),
            z
        }
    }
    Button {
        "Exit",
        "Exit"
    }
}

